import React from 'react';
import './contact.scss';

const Contact = () => {
    return (
        <section id="projects" className="contact section-padding">
            <h1 className="section-heading">Contact</h1>
        </section>
    );
};

export default Contact;
