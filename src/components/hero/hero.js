import React from 'react';
import './hero.scss';
import Particles from '../particles/particles';

const Hero = () => {
    return (
        <div id="home" className="hero w-100">
            <Particles />
        </div>
    );
};

export default Hero;
